<h1>本の一覧</h1>
<div class="row w-100">
  <% @books.each do |book| %>
  <div class="col-3 mb-5">
    <p><%= link_to '本のリンク', book.url %></p>
    <p><%= book.title %></p>
    <p><%= link_to '本の詳細', book_path(book.id) %></p>
  </div>
  <% end %>
</div>
